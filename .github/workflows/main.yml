name: Оновлення відсотків на сайті

on:
  push:
    branches:
      - master

jobs:
  update_percentages:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Update Percentages
      run: |
        cd tools
        python percents.py
        git config --global user.email "ihor@noleron.com"
        git config --global user.name "Ihor Andreew"
        git add .
        git commit -m "Оновлення відсотків на сайті"
        git push https://${{ secrets.NOLERON }}@github.com/ua-thinking/Starbound.git HEAD:main
